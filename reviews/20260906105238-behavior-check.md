# Behavior conformance check

**Scope:** lifecycle-engine.md pending-finalization replay assertion
**Behavior spec commit:** not inspected in this agent context (repository instructions prohibit git commands)

## Scorecard

Assertions checked: 1 - HOLDS: 1 - DIVERGES: 0 - UNDETERMINED: 0

## Findings (divergences)

None.

## Holds (verified conformant)

| Assertion ID | Evidence |
|--------------|----------|
| LE-§6-PENDING-FINALIZATION-REPLAY-IDENTITY | `specs/behavior/lifecycle-engine.md:1320` requires pending replay to re-resolve merge-unit and handoff task identity before mark-merged persistence. `src/gza/landing.py:1888` calls pending-task validation from final preflight after post-merge verification. `src/gza/landing.py:1968` reloads the merge unit and refuses changed mandatory identity. `src/gza/landing.py:2011` reloads each stored deferred-task ID and validates prompt, scope, review/implementation/finding identity, `urgent`, and `create_pr`. `src/gza/landing.py:2076` does the same for stored follow-up IDs. `src/gza/cli/land.py:228` persists merged state through `set_merge_unit_state_if_identity`, and `src/gza/db.py:14050` performs the conditional update. |

## Recommendations

None.

## Machine-readable findings

```json
[]
```
