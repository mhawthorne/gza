# Behavior conformance check

**Scope:** no-op improve assertions in `00-overview.md` and `lifecycle-engine.md`
**Behavior spec commit:** unavailable in agent context (git commands disabled by repo policy)

## Scorecard
Assertions checked: 3 — HOLDS: 3 · DIVERGES: 0 · UNDETERMINED: 0

## Assertions

- `OV-INV-NOOP-PARK` — Repeated no-op improves at `max_noop_improve_cycles` park to `needs_discussion`, except for the verify-evidence clear case.
- `LE-§6-VERIFY-CLEAR` — A verify-origin blocker is cleared only by runner-owned same-head review FAIL then no-op-improve PASS evidence, and lifecycle must not run a detached verify just to clear it.
- `LE-§6-NOOP-PARK-UNCONDITIONAL` — Otherwise, consecutive no-op improves at the bound park to `needs_discussion` with reason `improve-no-op`.

## Findings (divergences)

None.

## Holds (verified conformant)

| Assertion ID | Evidence |
|--------------|----------|
| `OV-INV-NOOP-PARK` | Spec says all non-exception no-op cases park at [specs/behavior/00-overview.md](/workspace/specs/behavior/00-overview.md:178). Engine enforces the threshold for fresh-comment loops at [src/gza/advance_engine.py](/workspace/src/gza/advance_engine.py:3062) and review loops at [src/gza/advance_engine.py](/workspace/src/gza/advance_engine.py:3112), both routing to the park action at [src/gza/advance_engine.py](/workspace/src/gza/advance_engine.py:796). |
| `LE-§6-VERIFY-CLEAR` | Spec requires runner-owned same-head fail→pass evidence and forbids detached verify clearing at [specs/behavior/lifecycle-engine.md](/workspace/specs/behavior/lifecycle-engine.md:188). Runner captures and checks that persisted no-op improve verify evidence through [src/gza/runner.py](/workspace/src/gza/runner.py:6827), and the no-op limit action only falls into the verify-blocked branch when the review is still unresolved, at [src/gza/advance_engine.py](/workspace/src/gza/advance_engine.py:828). |
| `LE-§6-NOOP-PARK-UNCONDITIONAL` | Spec says the bound now parks unconditionally at [specs/behavior/lifecycle-engine.md](/workspace/specs/behavior/lifecycle-engine.md:196). The park message and reason are emitted without any tag-based bypass at [src/gza/advance_engine.py](/workspace/src/gza/advance_engine.py:796), and the operator warning is now the unconditional text at [src/gza/runner.py](/workspace/src/gza/runner.py:3962). |

## Recommendations
1. No code/spec divergence found for the edited no-op improve contract.
