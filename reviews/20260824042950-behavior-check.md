# Behavior conformance check

**Scope:** lifecycle-engine.md §8a terminal-state branch
**Behavior spec commit:** not collected; repository instructions prohibit git commands in this agent context

## Scorecard
Assertions checked: 1 - HOLDS: 1 - DIVERGES: 0 - UNDETERMINED: 0

## Findings (divergences)

None.

## Undetermined (needs a closer look or a human)

None.

## Holds (verified conformant)

| Assertion ID | Evidence |
|--------------|----------|
| LE-§8A-TERMINAL-STATE-BRANCH | The spec defines `merged`, `empty`, and `redundant` as the complete terminal branch and permits only one writable terminal reconciliation mutation from `unmerged` before returning the known result at `specs/behavior/lifecycle-engine.md:823`. `LandTerminalResult` carries the owner, source, target, dry-run, and reconciliation evidence at `src/gza/landing.py:29`; the coordinator returns initially terminal units without reconciliation or downstream collaborators at `src/gza/landing.py:121`, keeps dry-run reconciliation query-only at `src/gza/landing.py:135`, persists writable `unmerged` to the proven terminal state with `expected_state="unmerged"` and refreshes before returning at `src/gza/landing.py:145`, and treats concurrent terminal refresh as a terminal result at `src/gza/landing.py:183`. The production merge-truth reconciler returns all three landed/no-work states from the canonical classifier at `src/gza/landing.py:285`. The CLI opens dry-runs in query-only mode at `src/gza/cli/land.py:25`, routes through the coordinator at `src/gza/cli/land.py:28`, and renders owner, source, target, and known outcome while preserving already-merged versus terminal no-work wording at `src/gza/cli/land.py:48`. Tests cover initially terminal states with zero downstream activity at `tests/cli/test_land.py:153`, writable and dry-run `unmerged -> merged` coordinator reconciliation at `tests/cli/test_land.py:207`, initial terminal CLI identity output at `tests/cli/test_land.py:283`, and production-classifier-backed writable/dry-run reconciliation for `merged`, `empty`, and `redundant` at `tests/cli/test_land.py:314`. |

## Recommendations

1. No behavior/spec divergences found in the scoped §8a terminal-state branch.

## Machine-readable findings

```json
[
  {
    "assertion_id": "LE-§8A-TERMINAL-STATE-BRANCH",
    "verdict": "HOLDS",
    "recommendation": null,
    "spec_file": "specs/behavior/lifecycle-engine.md",
    "spec_section": "§8a",
    "summary": "Terminal states merged, empty, and redundant are explicit idempotent land results with the permitted writable reconciliation mutation and no downstream landing side effects.",
    "evidence": [
      {
        "path": "specs/behavior/lifecycle-engine.md",
        "line": 823,
        "note": "Defines complete terminal-state handling and the single permitted writable terminal reconciliation mutation."
      },
      {
        "path": "src/gza/landing.py",
        "line": 29,
        "note": "Terminal result model carries owner, source, target, dry-run, and reconciliation evidence."
      },
      {
        "path": "src/gza/landing.py",
        "line": 121,
        "note": "Initially terminal units return immediately before reconciliation or downstream activity."
      },
      {
        "path": "src/gza/landing.py",
        "line": 135,
        "note": "Dry-run reconciliation returns a query-only terminal result without durable mutation."
      },
      {
        "path": "src/gza/landing.py",
        "line": 145,
        "note": "Writable reconciliation performs one compare-and-set transition from unmerged to the proven terminal state and refreshes before returning."
      },
      {
        "path": "src/gza/landing.py",
        "line": 183,
        "note": "Concurrent terminal refresh returns a terminal result instead of falling into ordinary work."
      },
      {
        "path": "src/gza/landing.py",
        "line": 285,
        "note": "Production merge-truth reconciliation returns any canonical landed/no-work terminal classification."
      },
      {
        "path": "src/gza/cli/land.py",
        "line": 25,
        "note": "Dry-run opens the store in query-only mode."
      },
      {
        "path": "src/gza/cli/land.py",
        "line": 48,
        "note": "Terminal output includes owner, source, target, known outcome, and distinct already-merged/no-work wording."
      },
      {
        "path": "tests/cli/test_land.py",
        "line": 153,
        "note": "Tests initially terminal states return known outcomes without state writes or downstream activity."
      },
      {
        "path": "tests/cli/test_land.py",
        "line": 207,
        "note": "Tests later reconciliation to merged has writable persistence, dry-run query-only behavior, and zero downstream activity."
      },
      {
        "path": "tests/cli/test_land.py",
        "line": 283,
        "note": "Tests initial terminal CLI output includes owner, source, target, and known outcome."
      },
      {
        "path": "tests/cli/test_land.py",
        "line": 314,
        "note": "Tests production-classifier-backed writable and dry-run reconciliation for merged, empty, and redundant."
      }
    ],
    "report_path": "reviews/20260824042950-behavior-check.md"
  }
]
```
