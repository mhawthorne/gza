# Behavior conformance check

**Scope:** lifecycle-engine.md §8a terminal-state branch
**Behavior spec commit:** not collected; repository instructions prohibit git commands in this agent context

## Scorecard
Assertions checked: 1 - HOLDS: 1 - DIVERGES: 0 - UNDETERMINED: 0

## Findings (divergences)

None.

## Undetermined (needs a closer look or a human)

None.

## Holds (verified conformant)

| Assertion ID | Evidence |
|--------------|----------|
| LE-§8A-TERMINAL-STATE-BRANCH | The spec defines `merged`, `empty`, and `redundant` as distinct terminal outcomes, while requiring persisted recorded-head `empty`/`redundant` rows to pass recorded-head no-work validation before they are returned as known terminal results at `specs/behavior/lifecycle-engine.md:823`. `LandTerminalResult` carries the owner, source, target, dry-run, and reconciliation evidence at `src/gza/landing.py:58`; the coordinator returns initially `merged` units authoritatively, routes recorded-head no-work units through reconciliation at `src/gza/landing.py:155`, restores positively stale no-work units to `unmerged` only for writable runs at `src/gza/landing.py:242`, keeps dry-run repair-needed query-only at `src/gza/landing.py:231`, re-proves writable terminal truth before persistence at `src/gza/landing.py:183`, and conditions the atomic update on proof identity at `src/gza/db.py:13167`. The production merge-truth reconciler returns terminal proof carrying durable identity and live source/target heads from the canonical classifier at `src/gza/landing.py:467`. The CLI opens dry-runs in query-only mode at `src/gza/cli/land.py:25`, routes through the coordinator at `src/gza/cli/land.py:28`, and renders owner, source, target, and known outcome while preserving already-merged versus terminal no-work wording at `src/gza/cli/land.py:48`. Tests cover initially terminal states with zero downstream activity at `tests/cli/test_land.py:204`, recorded-head no-work represented/missing/unavailable outcomes at `tests/cli/test_land.py:230`, stale durable identity and live-ref races at `tests/cli/test_land.py:418`, post-write terminal refresh idempotency at `tests/cli/test_land.py:522`, and production-classifier-backed writable/dry-run reconciliation for `merged`, `empty`, and `redundant` at `tests/cli/test_land.py:596`. |

## Recommendations

1. No behavior/spec divergences found in the scoped §8a terminal-state branch.

## Machine-readable findings

```json
[
  {
    "assertion_id": "LE-§8A-TERMINAL-STATE-BRANCH",
    "verdict": "HOLDS",
    "recommendation": null,
    "spec_file": "specs/behavior/lifecycle-engine.md",
    "spec_section": "§8a",
    "summary": "Terminal state handling distinguishes authoritative merged rows from recorded-head no-work rows that require validation, and writable terminal reconciliation is proof-identity-bound with no downstream landing side effects.",
    "evidence": [
      {
        "path": "specs/behavior/lifecycle-engine.md",
        "line": 823,
        "note": "Defines complete terminal-state handling, recorded-head no-work validation, and proof-bound writable terminal reconciliation."
      },
      {
        "path": "src/gza/landing.py",
        "line": 58,
        "note": "Terminal result model carries owner, source, target, dry-run, and reconciliation evidence."
      },
      {
        "path": "src/gza/landing.py",
        "line": 155,
        "note": "Initially merged units return authoritatively, while recorded-head no-work units route through no-work validation."
      },
      {
        "path": "src/gza/landing.py",
        "line": 231,
        "note": "Dry-run recorded-head repair reports a query-only nonterminal repair-needed result without durable mutation."
      },
      {
        "path": "src/gza/landing.py",
        "line": 183,
        "note": "Writable reconciliation re-proves terminal truth immediately before persistence."
      },
      {
        "path": "src/gza/landing.py",
        "line": 242,
        "note": "Writable recorded-head no-work repair restores positively stale terminal no-work rows to unmerged."
      },
      {
        "path": "src/gza/landing.py",
        "path": "src/gza/db.py",
        "line": 13167,
        "note": "Atomic terminal writes are conditioned on the durable merge-unit identity used by proof."
      },
      {
        "path": "src/gza/landing.py",
        "line": 467,
        "note": "Production merge-truth reconciliation returns terminal proof with durable identity and live source/target heads."
      },
      {
        "path": "src/gza/cli/land.py",
        "line": 25,
        "note": "Dry-run opens the store in query-only mode."
      },
      {
        "path": "src/gza/cli/land.py",
        "line": 48,
        "note": "Terminal output includes owner, source, target, known outcome, and distinct already-merged/no-work wording."
      },
      {
        "path": "tests/cli/test_land.py",
        "line": 204,
        "note": "Tests initially terminal states without recorded head return known outcomes without state writes or downstream activity."
      },
      {
        "path": "tests/cli/test_land.py",
        "line": 230,
        "note": "Tests recorded-head no-work represented, missing, and unavailable outcomes for writable and dry-run land."
      },
      {
        "path": "tests/cli/test_land.py",
        "line": 418,
        "note": "Tests stale durable identity and live source/target races reject stale terminal writes."
      },
      {
        "path": "tests/cli/test_land.py",
        "line": 522,
        "note": "Tests post-write refresh returns the authoritative terminal state instead of reporting persistence failure."
      },
      {
        "path": "tests/cli/test_land.py",
        "line": 596,
        "note": "Tests production-classifier-backed writable and dry-run reconciliation for merged, empty, and redundant."
      }
    ],
    "report_path": "reviews/20260824042950-behavior-check.md"
  }
]
```
