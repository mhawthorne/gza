# Behavior conformance check

**Scope:** lifecycle-engine.md §8a terminal-state branch
**Behavior spec commit:** not collected; repository instructions prohibit git commands in this agent context

## Scorecard
Assertions checked: 1 - HOLDS: 0 - DIVERGES: 0 - UNDETERMINED: 1

## Findings (divergences)

None.

## Undetermined (needs a closer look or a human)

| Assertion ID | Why undetermined |
|--------------|------------------|
| LE-§8A-TERMINAL-STATE-BRANCH | The spec now requires `gza land` to handle `merged`, `empty`, and `redundant` as explicit idempotent terminal results with no rebase/provider/verify/review/judge/materialization/merge side effects, but the current landing CLI is still a placeholder and no coordinator implementation exists to inspect. |

## Holds (verified conformant)

None.

## Recommendations

1. Implement the landing coordinator against the §8a terminal-state branch before treating `gza land` as runtime-conformant.

## Machine-readable findings

```json
[
  {
    "assertion_id": "LE-§8A-TERMINAL-STATE-BRANCH",
    "verdict": "UNDETERMINED",
    "recommendation": null,
    "spec_file": "specs/behavior/lifecycle-engine.md",
    "spec_section": "§8a",
    "summary": "Terminal states merged, empty, and redundant must be explicit idempotent land results with no execution or merge side effects.",
    "evidence": [
      {
        "path": "specs/behavior/lifecycle-engine.md",
        "line": 823,
        "note": "Spec requires the complete terminal-state branch before ordinary unmerged eligibility."
      },
      {
        "path": "specs/behavior/lifecycle-engine.md",
        "line": 1115,
        "note": "Spec requires dry-run to report known terminal states without crossing an execution boundary."
      },
      {
        "path": "src/gza/cli/land.py",
        "line": 6,
        "note": "The land command is currently a placeholder, so runtime conformance cannot be verified."
      }
    ],
    "report_path": "reviews/20260824042950-behavior-check.md"
  }
]
```
