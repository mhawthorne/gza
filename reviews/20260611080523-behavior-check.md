# Behavior conformance check

**Scope:** Slice C read-side ref batching against `specs/behavior/lifecycle-engine.md`
**Behavior spec commit:** unavailable in agent context

## Scorecard
Assertions checked: 3 — HOLDS: 3 · DIVERGES: 0 · UNDETERMINED: 0

## Findings (divergences)

None.

## Holds (verified conformant)

| Assertion ID | Evidence |
|--------------|----------|
| LE-ORDERED-RULES-ONE-PATH | `src/gza/lineage_query.py:806` only preloads git cache inputs before the existing `determine_next_action(...)` call later in the same function; rule evaluation still flows through the shared lifecycle engine at `src/gza/lineage_query.py:1098`. |
| LE-§3-FAIL-CLOSED | Both planning preload helpers fail open and do not synthesize lifecycle outcomes: `src/gza/lineage_query.py:749` and `src/gza/cli/git_ops.py:209` wrap batching in `try/except` and only log, so git-read optimization cannot bypass the strict-scope / safety gates that the existing rule path evaluates. |
| LE-P4-LOCAL-TARGET | Batched ref resolution remains local-target-only. `src/gza/git.py:949` resolves explicit local refs via `git cat-file --batch-check`, `src/gza/git.py:483` resolves local branches from `refs/heads/`, and `src/gza/git.py:866` still chooses between the local branch and `origin/<branch>` using the same merge-source logic instead of introducing any new remote fetch or alternate merge policy. |

## Recommendations
1. No behavior divergence found for Slice C. Keep future batching changes constrained to cache priming and shared helper reuse.
