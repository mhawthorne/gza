# Behavior conformance check

**Scope:** `OV-INV1-REBASE-SINGLETON-BRANCH`
**Behavior spec commit:** unavailable in this agent context; direct git commands are prohibited by `AGENTS.md`

## Scorecard
Assertions checked: 1 - HOLDS: 1 · DIVERGES: 0 · UNDETERMINED: 0

## Findings (divergences)
None.

## Undetermined (needs a closer look or a human)

| Assertion ID | Why undetermined |
|--------------|------------------|
| None | None |

## Holds (verified conformant)

| Assertion ID | Evidence |
|--------------|----------|
| OV-INV1-REBASE-SINGLETON-BRANCH | `00-overview.md` invariant 1 requires active rebase singletons to be keyed by source branch and duplicate creation not to consume capacity. `SqliteTaskStore._add_task_conn` resolves a branch key before insert under the guarded add transaction and raises `DuplicateActiveChildError` when an active row for that branch exists. `_resolve_new_rebase_target_branch_conn` returns an explicit rebase branch directly. `get_active_rebase_tasks_resolving_to_branch` queries active branchful rebase rows by `tasks.branch` and only uses lineage resolution for branchless legacy rows. Regression tests cover cross-parent same-branch duplicates, independent different branches, legacy branchless fallback, and parent-branch mutation not changing an active rebase key. |

## Recommendations
None.

## Machine-readable findings

```json
[
  {
    "assertion_id": "OV-INV1-REBASE-SINGLETON-BRANCH",
    "verdict": "HOLDS",
    "recommendation": null,
    "spec_file": "specs/behavior/00-overview.md",
    "spec_section": "INV1",
    "summary": "Active rebase singleton identity is enforced by source branch, with legacy lineage fallback only for branchless rows.",
    "evidence": [
      {
        "path": "src/gza/db.py",
        "line": 5404,
        "note": "Guarded rebase creation resolves the singleton branch key inside the add transaction before insert."
      },
      {
        "path": "src/gza/db.py",
        "line": 6322,
        "note": "An explicit new rebase branch is returned directly as the singleton key."
      },
      {
        "path": "src/gza/db.py",
        "line": 6343,
        "note": "Active branchful rebase rows are compared by tasks.branch; branchless rows remain eligible for legacy lineage fallback."
      },
      {
        "path": "tests/test_db.py",
        "line": 697,
        "note": "Regression covers different non-branchless parents whose rebase rows specify the same branch."
      },
      {
        "path": "tests/test_db.py",
        "line": 740,
        "note": "Regression covers parent branch mutation not changing the active rebase singleton key."
      }
    ],
    "report_path": "reviews/20260817232539-behavior-check.md"
  }
]
```
