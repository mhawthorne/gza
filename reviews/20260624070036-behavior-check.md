# Behavior conformance check

**Scope:** `specs/behavior/lineage.md` merge-unit owner/proven-merged invariant
**Invocation:** manual fallback for `/gza-behavior-check specs/behavior/lineage.md`
**Behavior spec commit:** unavailable in agent context

## Scorecard
Assertions checked: 2 — HOLDS: 2 · DIVERGES: 0 · UNDETERMINED: 0

## Findings (divergences)

None.

## Undetermined (needs a closer look or a human)

None.

## Holds (verified conformant)

| Assertion ID | Evidence |
|--------------|----------|
| `LINEAGE-MU-TERMINAL-FROM-ANY-MEMBER` | The new invariant in `specs/behavior/lineage.md` requires proven-merged truth from any merge-unit member to resolve the active unit terminally. `watch` now detects the stable `advance_reason` marker on the proven-merged skip at `src/gza/advance_engine.py:3436`-`3445`, routes that case through `_maybe_repair_target_already_merged_skip` at `src/gza/cli/watch.py:221`-`257`, and invokes `reconcile_task_branch_merge_truth(...)` from the skip path at `src/gza/cli/watch.py:2069`-`2081`, which expands the task to its branch cohort and persists terminal merge truth via `src/gza/sync_ops.py:818`-`845`. |
| `LINEAGE-MU-MERGED-PROVENANCE-STAYS-OWNER` | The invariant requires landed provenance to remain attributed to the canonical owner. The pre-existing owner gate remains intact in `_resolve_and_persist_post_merge_rebase_state` at `src/gza/advance_engine.py:574`-`600`, so non-owner follow-ups still do not persist ownership locally, while the shared reconcile path persists merge-unit state through `_persist_branch_updates(...)` and `set_merge_unit_state(...)`, which defaults `merged_by_task_id` to the unit owner. This is exercised directly by `tests/test_sync_ops.py::test_reconcile_task_branch_merge_truth_by_same_branch_improve_marks_owner_unit_merged`. |

## Recommendations
1. Keep future "already merged" repairs on the shared reconcile path so watch, sync, and merge-unit provenance continue to agree.
