# Behavior conformance check

**Scope:** `specs/behavior/isolated-merge-checkout.md`
**Behavior spec commit:** not recorded in this run (`git` commands are disabled in agent context)

## Scorecard
Assertions checked: 6 — HOLDS: 6 · DIVERGES: 0 · UNDETERMINED: 0

## Findings (divergences)

None.

## Undetermined (needs a closer look or a human)

None.

## Holds (verified conformant)

| Assertion ID | Evidence |
|--------------|----------|
| IMC-§1-DETACHED-STAGING | `ensure_watch_main_checkout(...)` refreshes a dedicated detached integration checkout and rejects attachment to `refs/heads/<default>` at `src/gza/cli/git_ops.py:764-795`. |
| IMC-§2-PROMOTION-MARKS-LANDED | Isolated merges are promoted before merge state flips to `merged` in `_execute_merge_action(...)` at `src/gza/cli/git_ops.py:2931-2938`. |
| IMC-§3-RESET-ATTACHED-CHECKOUT | Promotion resets the attached default-branch checkout to the advanced ref at `src/gza/cli/git_ops.py:839-840`. |
| IMC-§4-TRACKED-CHANGES-DO-NOT-BLOCK | Dirty attached checkouts now stash tracked changes instead of raising at `src/gza/cli/git_ops.py:830-833`, using tracked-only stash helpers at `src/gza/git.py:1352-1385`. |
| IMC-§6-CLEAN-RESTORE-RIDES-ONTO-NEW-TIP | Clean restore replays the stash after the reset and logs that outcome at `src/gza/cli/git_ops.py:841-849`. |
| IMC-§7-CONFLICT-PARKS-STASH-WITHOUT-REDIRTYING | Conflicting restore leaves the stash parked, keeps the checkout clean, and warns at `src/gza/cli/git_ops.py:850-860`; the helper undoes partial apply state with `reset --hard HEAD` at `src/gza/git.py:1373-1385`. |

## Recommendations
1. Keep future isolated-promotion changes pinned to `isolated-merge-checkout.md` so dirty-attached-checkout handling does not drift back into the broader watch spec.
