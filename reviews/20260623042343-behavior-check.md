# Behavior conformance check

**Scope:** `lifecycle-engine.md §8` and `watch-supervisor.md §1` local-main integration verify assertions
**Behavior spec commit:** unavailable in agent context (git commands not used)

## Scorecard
Assertions checked: 4 — HOLDS: 4 · DIVERGES: 0 · UNDETERMINED: 0

## Findings (divergences)

None.

## Holds (verified conformant)

| Assertion ID | Evidence |
|--------------|----------|
| `LE-§8-MAIN-VERIFY-RERUN-ON-FINGERPRINT-CHANGE` | `src/gza/main_integration_verify.py:236-262` reuses the stored fingerprint/head SHA and reruns verify when the current local-target tree no longer matches; `src/gza/cli/watch.py:1950-1959` and `src/gza/cli/git_ops.py:3364-3370` invoke that gate before merge execution. |
| `LE-§8-MAIN-VERIFY-HALT-FURTHER-MERGES` | `src/gza/cli/watch.py:2008-2015` skips later merge actions once the shared check reports `merges_halted`; `src/gza/cli/git_ops.py:3446-3453` does the same for `gza advance`. |
| `LE-RC-main-integration-verify-red` | `src/gza/main_integration_verify.py:23-25` defines the durable reason code; `src/gza/lineage_query.py:1389-1420` projects it into the owner-row/incomplete surface; `src/gza/cli/git_ops.py:3371-3378` and `src/gza/cli/watch.py:1960-1966` surface the same reason through operator attention paths. |
| `WS-§1-WATCH-PHASE3-POST-MERGE-INTEGRATION-VERIFY` | `src/gza/cli/watch.py:1944-1966` runs the shared local-target verify gate before same-cycle merge work, and `src/gza/cli/watch.py:2088-2104` reruns it after each successful merge, emitting durable attention when the result is not `passed`. |

## Recommendations
1. No behavior/spec divergences found in the scoped assertions.
