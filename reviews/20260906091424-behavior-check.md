# Behavior conformance check

**Scope:** lifecycle-engine.md §8a pending-finalization dry-run assertion
**Behavior spec commit:** not inspected; git commands are disabled by repository instructions for this agent context.

## Scorecard
Assertions checked: 1 - HOLDS: 1 - DIVERGES: 0 - UNDETERMINED: 0

## Findings (divergences)

None.

## Holds (verified conformant)

| Assertion ID | Evidence |
|--------------|----------|
| LE-§8A-LAND-DRY-RUN-PENDING-FINALIZATION | `specs/behavior/lifecycle-engine.md:1353` requires exact pending-finalization dry-run to report pending state and conditional checkpoint/finalization without invoking collaborators or writing state. `src/gza/landing.py:1438` branches on `request.dry_run` before `_run_post_merge_verify_phase` or `finalize_merge`, emits conditional `post_merge_verify`/`merge` steps at `src/gza/landing.py:1452` and `src/gza/landing.py:1464`, and returns a non-merged `LandResult` at `src/gza/landing.py:1476`. The CLI opens dry-run stores with `open_mode="query_only"` at `src/gza/cli/land.py:56` and routes pending-finalization units to the coordinator at `src/gza/cli/land.py:72`. |

## Recommendations

None.

## Machine-readable findings

```json
[]
```
