# Behavior conformance check

**Scope:** `LIN-P6-TERMINAL-LANDED-NOT-ACTIONABLE`
**Behavior spec commit:** not captured in agent context (`git` commands are disabled by repo policy)

## Scorecard
Assertions checked: 1 — HOLDS: 1 · DIVERGES: 0 · UNDETERMINED: 0

## Findings (divergences)

None.

## Undetermined (needs a closer look or a human)

None.

## Holds (verified conformant)

| Assertion ID | Evidence |
|--------------|----------|
| `LIN-P6-TERMINAL-LANDED-NOT-ACTIONABLE` | The contract requires terminal landed/no-work owner rows to stay hidden unless a failed descendant still has unique unmerged work or that absence cannot be proven at [specs/behavior/lineage.md](/workspace/specs/behavior/lineage.md:74). `src/gza/lineage_query.py` now enforces that with `_failed_leaf_has_unique_unmerged_work_under_terminal_owner(...)`, which suppresses same-unit, same-branch, and branchless failed leaves under terminal owners while keeping distinct or unprovable leaves visible at [src/gza/lineage_query.py](/workspace/src/gza/lineage_query.py:767) and uses that predicate before admitting failed leaves into owner rows at [src/gza/lineage_query.py](/workspace/src/gza/lineage_query.py:1232). The terminal-owner short-circuit still drops the owner row when no matching failed leaf survives at [src/gza/lineage_query.py](/workspace/src/gza/lineage_query.py:1292). Regression coverage proves both sides of the boundary: moot failed `implement` leaves under a redundant owner are suppressed at [tests/test_lineage_query.py](/workspace/tests/test_lineage_query.py:2143), distinct failed leaves with their own unmerged work keep the terminal owner visible at [tests/test_lineage_query.py](/workspace/tests/test_lineage_query.py:2798), and the pre-existing recovery-lane owner case stays visible only when recovery policy still marks the failed owner live at [tests/test_lineage_query.py](/workspace/tests/test_lineage_query.py:2190). |

## Recommendations
1. None.
