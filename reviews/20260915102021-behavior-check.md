# Behavior conformance check

**Scope:** scoped check for `main-verify-self-heal.md` MV4/MV5 and `lineage.md` owner-drop cascade assertions changed by this task
**Behavior spec commit:** `13bf072e5`

## Scorecard
Assertions checked: 5 - HOLDS: 5 - DIVERGES: 0 - UNDETERMINED: 0

## Findings (divergences)

None found in this scoped pass.

## Holds (verified conformant)

| Assertion ID | Evidence |
|--------------|----------|
| MV-MV4-FAILED-PHASE-COLLECTION | Structured failed phase results are collected into `failing_phases` and persisted as a list at `src/gza/main_integration_verify.py:1630` and `src/gza/main_integration_verify.py:1244`. |
| MV-MV4-CANONICAL-PHASE-IDENTITY | Multi-phase identities canonicalize to `phases:<sorted names>` in `src/gza/main_verify_scope.py:26` and `src/gza/main_verify_scope.py:33`. |
| MV-MV4-PER-PHASE-PROMPT-EVIDENCE | Main verify prompts render a plural failing-phase line and phase-local evidence sections in `src/gza/cli/watch.py:520`; phase evidence is built for every failing phase in `src/gza/main_integration_verify.py:687`. |
| MV-MV4-AFFIRMATIVE-PHASE-RETIREMENT | Verify state carries compact phase outcomes in `src/gza/main_integration_verify.py:1245`; watch cleanup uses the plural phase state rather than a singular phase field in `src/gza/cli/watch.py:7523`. |
| LIN-OWNER-DROP-SAME-SCOPE-CASCADE | Deliberate drops route through `drop_task_with_scope_cascade`, which includes same merge-unit members and same-unit `verify_fix` descendants at `src/gza/status_ops.py:21`; manual status uses the shared helper at `src/gza/status_ops.py:115`, and stale-unmerged execution reports cascaded transitioned IDs at `src/gza/cli/query.py:3530`. |

## Undetermined (needs a closer look or a human)

None.

## Recommendations

1. Keep future main-verify remediation changes routed through `main_verify_scope.py` so prompt, watch, and formatting code do not grow separate phase-set rules.
2. If the drop cascade grows beyond same-unit members and same-unit `verify_fix` descendants, add another behavior-check pass against `lineage.md` P3/P4/P5 before broadening the closure.

## Machine-readable findings

```json
[]
```
