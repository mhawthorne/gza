# Behavior conformance check

**Scope:** `LE-S9-manual-merge-non-rebase`
**Behavior spec commit:** not inspected; repository instructions prohibit git commands in this context

## Scorecard

Assertions checked: 1 - HOLDS: 1 - DIVERGES: 0 - UNDETERMINED: 0

## Findings (divergences)

None.

## Undetermined (needs a closer look or a human)

None.

## Holds (verified conformant)

| Assertion ID | Evidence |
|--------------|----------|
| LE-S9-manual-merge-non-rebase | `specs/behavior/lifecycle-engine.md:727` requires manual `gza merge` to have no `--rebase`, `--remote`, or `--resolve` compatibility surface and to reject those flags in the CLI parser. `src/gza/cli/main.py:1495` defines the merge parser options without those flags, while `src/gza/cli/main.py:1603` keeps the standalone `gza rebase` command and its execution-mode flags. `src/gza/cli/git_ops.py:1441` now checks only the remaining `--ignore-verify-gate`/`--force` relationship, with no fallback parsing for removed merge rebase flags. `tests/cli/test_main.py:1494` asserts argparse rejects each removed merge flag. |

## Recommendations

None.

## Machine-readable findings

```json
[
  {
    "assertion_id": "LE-S9-manual-merge-non-rebase",
    "verdict": "HOLDS",
    "recommendation": null,
    "spec_file": "specs/behavior/lifecycle-engine.md",
    "spec_section": "S9",
    "summary": "Manual merge has no rebase flag compatibility surface; removed flags are rejected by argparse and standalone rebase remains separate.",
    "evidence": [
      {
        "path": "src/gza/cli/main.py",
        "line": 1495,
        "note": "Merge parser defines manual merge options without --rebase, --remote, or --resolve."
      },
      {
        "path": "src/gza/cli/main.py",
        "line": 1603,
        "note": "Standalone rebase command remains defined separately with execution mode support."
      },
      {
        "path": "src/gza/cli/git_ops.py",
        "line": 1441,
        "note": "Manual merge option relationship validation no longer reads removed rebase flag attributes."
      },
      {
        "path": "tests/cli/test_main.py",
        "line": 1494,
        "note": "Parser-level test covers rejection of --rebase, --remote, and --resolve on gza merge."
      }
    ],
    "report_path": "reviews/20260824164914-behavior-check.md"
  }
]
```
