# Behavior conformance check

**Scope:** `lifecycle-engine.md` — `LE-§6-VERIFY-ONLY-CLEAR`
**Behavior spec commit:** unavailable in agent context
**Contract status:** `lifecycle-engine.md` is draft overall; this check covers the new §6 normative principle text.

## Scorecard
Assertions checked: 1 — HOLDS: 1 · DIVERGES: 0 · UNDETERMINED: 0

## Findings (divergences)

None.

## Holds (verified conformant)

| Assertion ID | Evidence |
|--------------|----------|
| `LE-§6-VERIFY-ONLY-CLEAR` | `src/gza/runner.py` requires verify-only review classification plus runner-owned review-time failure evidence at the same branch/head, then requires persisted passing improve-side verify evidence captured after review completion with matching branch and head SHA before clearing review state. `src/gza/runner.py` reruns `verify_command` from the no-op improve's own worktree and persists the result on the improve row only after that same-head failed-review gate applies. `src/gza/advance_engine.py` follows the same fail-closed semantics: it only repairs skipped review clears from verify-only same-head review-fail/no-op-improve-pass evidence, persists `clear_review_state(...)`, and otherwise parks the no-op limit instead of launching any detached re-verify action. Those checks govern verify-only blockers caused by ordinary `verify_command` failures as well as timeouts. |

## Recommendations
1. Keep future §6 edits aligned with the two-part gate: verify-only review classification plus runner-owned same-head review-fail/no-op-improve-pass provenance checks, for ordinary verify failures and timeouts alike, and with the fail-closed no-op parking path in `src/gza/advance_engine.py`.
