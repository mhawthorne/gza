# Behavior conformance check

**Scope:** `lifecycle-engine.md` — `LE-§6-VERIFY-ONLY-REVIEW-CLEAR-INVARIANT`
**Behavior spec commit:** unavailable in agent context
**Contract status:** `lifecycle-engine.md` is draft overall; this check covers the new §6 normative principle text.

## Scorecard
Assertions checked: 1 — HOLDS: 1 · DIVERGES: 0 · UNDETERMINED: 0

## Findings (divergences)

None.

## Holds (verified conformant)

| Assertion ID | Evidence |
|--------------|----------|
| `LE-§6-VERIFY-ONLY-REVIEW-CLEAR-INVARIANT` | `src/gza/runner.py:4029-4041` requires persisted passing improve-side verify evidence, captured after review completion, with matching branch and head SHA; `src/gza/runner.py:4051-4059` requires runner-owned review-time failure evidence at the same branch/head; `src/gza/runner.py:4062-4097` clears review state only when both provenance checks hold; `src/gza/runner.py:4117-4205` reruns `verify_command` from the no-op improve's own worktree and persists the result on the improve row only after that same-head failed-review gate applies; `src/gza/advance_engine.py:834-844` parks the no-op improve limit instead of launching any detached re-verify action. |

## Recommendations
1. No code change required for this assertion. Keep future §6 edits aligned with the runner-owned provenance checks in `src/gza/runner.py`, especially the same-head failed-review gate for no-op improve re-verification, and the fail-closed no-op parking path in `src/gza/advance_engine.py`.
