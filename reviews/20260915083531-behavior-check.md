# Behavior Conformance Check

**Scope:** `specs/behavior/shared-schema-isolation.md`
**Behavior spec commit:** `2db76abf5`
**Checked:** 2026-09-15 08:35 UTC

## Assertion Inventory

- `SSI1-EVALUATED-NO-SHARED-SCHEMA-MUTATION`
- `SSI2-CANONICAL-AUTHORITY-PROOF`
- `SSI3-MISSING-AUTHORITY-NO-CURRENT-SCHEMA-MUTATION`
- `SSI4-PRIVATE-COPIES-MAY-MIGRATE`
- `SSI5-PROVIDER-VERIFY-ISOLATION`
- `SSI6-SCHEMA-SKEW-UNAVAILABLE`

## Scorecard

Assertions checked: 6
HOLDS: 4
DIVERGES: 0
UNDETERMINED: 2

## Findings

None.

## Holds

| Assertion ID | Evidence |
| --- | --- |
| `SSI1-EVALUATED-NO-SHARED-SCHEMA-MUTATION` | Shared stores opened without required authority are downgraded to `defer_shared` before construction (`src/gza/db.py:6161`) and the deferred path validates through a read-only SQLite URI instead of migrating (`src/gza/db.py:6678`). |
| `SSI2-CANONICAL-AUTHORITY-PROOF` | The proof binds canonical root, default branch, and HEAD (`src/gza/migration_authority.py:89`) after checking the primary worktree (`src/gza/migration_authority.py:60`), current branch (`src/gza/migration_authority.py:66`), and `HEAD == refs/heads/<default-branch>` (`src/gza/migration_authority.py:73`). The DB layer revalidates inside the bootstrap lock before write phases (`src/gza/db.py:6641`). CLI and runtime opening pass authority before shared read-write opens (`src/gza/cli/_common.py:271`, `src/gza/db.py:527`). |
| `SSI3-MISSING-AUTHORITY-NO-CURRENT-SCHEMA-MUTATION` | Missing authority with a shared policy selects `defer_shared` (`src/gza/db.py:6161`), and the deferred path skips bootstrap, startup repairs, migrations, and registration by inspecting/validating read-only (`src/gza/db.py:6678`). |
| `SSI4-PRIVATE-COPIES-MAY-MIGRATE` | Config-derived local/private DB paths still resolve to `auto_private` (`src/gza/db.py:5934`), so the authority downgrade applies only to non-private/shared opens (`src/gza/db.py:6165`). |

## Undetermined

| Assertion ID | Reason |
| --- | --- |
| `SSI5-PROVIDER-VERIFY-ISOLATION` | This implementation slice did not audit or change every provider and verify subprocess launch path; those guarantees are broader than canonical migration-authority wiring. |
| `SSI6-SCHEMA-SKEW-UNAVAILABLE` | This implementation slice did not audit or change the full main/lifecycle verify result normalization path; schema-skew classification is outside the canonical authority proof. |

## Machine-Readable Summary

```json
{
  "scope": "specs/behavior/shared-schema-isolation.md",
  "behavior_spec_commit": "2db76abf5",
  "checked_at": "2026-09-15T08:35:31Z",
  "assertions_checked": 6,
  "holds": 4,
  "diverges": 0,
  "undetermined": 2,
  "findings": [],
  "verdicts": {
    "SSI1-EVALUATED-NO-SHARED-SCHEMA-MUTATION": "HOLDS",
    "SSI2-CANONICAL-AUTHORITY-PROOF": "HOLDS",
    "SSI3-MISSING-AUTHORITY-NO-CURRENT-SCHEMA-MUTATION": "HOLDS",
    "SSI4-PRIVATE-COPIES-MAY-MIGRATE": "HOLDS",
    "SSI5-PROVIDER-VERIFY-ISOLATION": "UNDETERMINED",
    "SSI6-SCHEMA-SKEW-UNAVAILABLE": "UNDETERMINED"
  }
}
```
