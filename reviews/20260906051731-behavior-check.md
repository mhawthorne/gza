# Behavior conformance check

**Scope:** focused landing merge/post-merge verification assertions
**Behavior spec commit:** not recorded; repository AGENTS.md forbids git commands in this context

## Scorecard

Assertions checked: 4 - HOLDS: 4 - DIVERGES: 0 - UNDETERMINED: 0

## Findings (divergences)

None.

## Holds (verified conformant)

| Assertion ID | Evidence |
|--------------|----------|
| LE-§8A-FINAL-PREFLIGHT-BEFORE-SIDE-EFFECTS | `specs/behavior/lifecycle-engine.md:1292` requires exact source/target/clean checkout/clean merge rechecks before materialization or merge. `src/gza/landing.py:1046` calls `_final_preflight_block` before `execute_merge`, and `src/gza/landing.py:1196` checks source SHA, target SHA, current checkout, dirty checkout, and clean merge proof. |
| LE-§8A-LAND-PROVENANCE | `specs/behavior/lifecycle-engine.md:1318` requires `manual_land` for non-escalated land and `manual_land_escalated` for guarded escalation. `src/gza/landing.py:1079` selects those two provenance labels, `src/gza/cli/land.py:179` passes the label to the shared merge path, and `src/gza/db.py:5830` includes both labels in validated merge-source values. |
| LE-§8A-POST-MERGE-CHECKPOINT | `specs/behavior/lifecycle-engine.md:1323` requires refreshing or reusing the canonical post-merge target checkpoint before reporting the authoritative merged result. `src/gza/landing.py:1160` runs the post-merge verifier after merge success and before returning clean success; `src/gza/cli/land.py:209` wires it to `check_main_integration_verify`, which reuses or refreshes the canonical checkpoint. |
| LE-§8A-POST-MERGE-FAILURE-WORDING | `specs/behavior/lifecycle-engine.md:1449` says post-merge checkpoint failure is non-success but not `LandBlocked` or `Cannot land`. `src/gza/landing.py:1161` returns a merged `LandResult` with `post_merge_verify_failure`, and `src/gza/cli/land.py:256` renders `Merged <task>, but integration verification failed: <fact>.` with exit code 1. |

## Recommendations

No follow-up required for this slice.

## Machine-readable findings

```json
[]
```
