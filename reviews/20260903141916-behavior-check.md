# Behavior conformance check

**Scope:** lifecycle-engine.md §8a landing coordinator slice
**Behavior spec commit:** not collected; repository policy forbids git commands in this agent context

## Scorecard

Assertions checked: 5 - HOLDS: 5 - DIVERGES: 0 - UNDETERMINED: 0

## Findings (divergences)

None.

## Holds (verified conformant)

| Assertion ID | Evidence |
|--------------|----------|
| LE-8A-RESOLVE-CANONICAL-UNIT | `specs/behavior/lifecycle-engine.md:913` requires resolving the selected task through canonical merge-unit membership. `src/gza/landing.py:450` uses the query-only merge subject resolver, fails closed on diagnostics or missing active unit at `src/gza/landing.py:471`, and records owner/source/target identity at `src/gza/landing.py:563`. |
| LE-8A-ALREADY-LANDED-IDEMPOTENT | `specs/behavior/lifecycle-engine.md:917` requires idempotent already-merged completion. `src/gza/landing.py:551` reconciles merge truth through the shared task-scoped path and `src/gza/landing.py:388` returns `already_merged=True` without later execution phases. |
| LE-8A-DRY-RUN-QUERY-ONLY | `specs/behavior/lifecycle-engine.md:1192` forbids dry-run mutations and requires stopping at the first execution-required boundary. `src/gza/cli/land.py:18` opens the store read-only for dry-run, `src/gza/landing.py:346` disables reconciliation persistence, and `src/gza/landing.py:407` emits conditional dry-run steps using the shared planner. |
| LE-8A-BOUNDED-TRANSITIONS | `specs/behavior/lifecycle-engine.md:1201` requires a named transition-limit policy and visited-state detector. `src/gza/landing.py:298` defines `LandingTransitionLimitPolicy`, and `src/gza/landing.py:330` enforces both cap and repeated-fingerprint refusal. |
| LE-8A-TYPED-ONE-FACT-REFUSAL | `specs/behavior/lifecycle-engine.md:1235` requires stable `LandBlocked` refusals with evidence refs. `src/gza/landing.py:421` maps execution boundaries to one typed block, while identity and dirty-checkout failures are selected before lower-precedence work at `src/gza/landing.py:459` and `src/gza/landing.py:544`. |

## Recommendations

1. Continue later landing slices by replacing the execution-required stop points with shared rebase, verify, review, judge, materialization, and merge services.

## Machine-readable findings

```json
[]
```
