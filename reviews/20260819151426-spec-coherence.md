# Behavior spec coherence check

**Scope:** changed files (`specs/behavior/lifecycle-engine.md`)
**Owner map reviewed:** `specs/behavior/README.md`, `specs/behavior/00-overview.md`, `specs/behavior/lifecycle-engine.md`

## Summary
Files reviewed: 3
Findings: 0 overlap · 0 restatement · 0 cross-reference · 0 normative-discipline · 0 open-question/implementation-note issues · 0 plain-language rewrites

Ownership map:

| File | Responsibility |
|------|----------------|
| `specs/behavior/README.md` | Index and writing convention; does not define behavior. |
| `specs/behavior/00-overview.md` | Shared vocabulary, system-wide invariants, lifecycle diagram, and human-escalation table. |
| `specs/behavior/lifecycle-engine.md` | Engine-only ordered decision rules, policy knobs, parked reason codes, and ratified decisions. |

The changed read-only/query repair contract belongs in `lifecycle-engine.md` because it defines how the lifecycle engine evaluates stale rebase and resolution-review metadata on writable versus query-only paths. The surrounding file already cross-references `00-overview.md` for shared vocabulary and invariants, so the new text does not duplicate ownership from the overview.

## Blockers
None.

## Follow-Ups
None.

## Questions / Assumptions
None.

## Verdict
Verdict: APPROVED
