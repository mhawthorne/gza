# Behavior conformance check

**Scope:** `specs/behavior/watch-supervisor.md`
**Invocation:** `/gza-behavior-check specs/behavior/watch-supervisor.md`
**Behavior spec commit:** unavailable in agent context

## Scorecard
Assertions checked: 3 — HOLDS: 3 · DIVERGES: 0 · UNDETERMINED: 0

## Findings (divergences)

None.

## Holds (verified conformant)

| Assertion ID | Evidence |
|--------------|----------|
| WS-S6-PER-CYCLE-HUMAN-REQUIRED-VISIBILITY | `watch` now recomputes the shared recovery-lane owner mapping each cycle and emits failed-recovery needs-attention rows from the recovery skip branch regardless of `show_skipped`: `src/gza/cli/watch.py:2280-2323`. |
| WS-S6-OWNER-BASED-PARITY | Failed-recovery attention is re-rooted to the owner row and carries failed-leaf detail via `_reroot_failed_recovery_attention_action(...)`, then both the advance-attention path and recovery-skip path reuse the same owner-based key/message shape: `src/gza/cli/watch.py:329-367`, `src/gza/cli/watch.py:1891-1915`, `src/gza/cli/watch.py:2297-2323`. |
| WS-S6-PER-CYCLE-SUMMARY | Visible attention still feeds the shared `Needs attention (...)` roundup every cycle through `emit_attention(...)` and `_emit_cycle_attention_summary(...)`: `src/gza/cli/watch.py:944-950`, `src/gza/cli/watch.py:1384-1391`. |

## Recommendations
1. Keep the owner-based recovery-lane tests in `tests/cli/test_watch.py` aligned with the `collect_recovery_lane_entries(...)` surface when future recovery reasons are added.
