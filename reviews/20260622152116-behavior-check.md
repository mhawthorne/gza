# Behavior conformance check

**Scope:** `watch-supervisor.md` merge logging assertions (`§1` phase-3/phase-5 merge rules)
**Behavior spec commit:** unavailable in agent context

## Scorecard
Assertions checked: 3 — HOLDS: 3 · DIVERGES: 0 · UNDETERMINED: 0

## Holds (verified conformant)

| Assertion ID | Evidence |
|--------------|----------|
| `WS-§1-MERGE-ONE-LINE-PER-UNIT` | `_resolve_watch_merge_log_event()` resolves a merge-unit member to the owner task and a unit-scoped dedupe key at [src/gza/cli/watch.py](/workspace/src/gza/cli/watch.py:1078); inline dry-run and live merge logging both emit `MERGE` from that resolved owner only at [src/gza/cli/watch.py](/workspace/src/gza/cli/watch.py:1996) and [src/gza/cli/watch.py](/workspace/src/gza/cli/watch.py:2043). |
| `WS-§1-MERGE-BEFORE-WORKER-STARTS` | Direct merge actions still run in the phase-3 lifecycle block before any worker-start handling, and the inline `MERGE` emit remains inside that block before follow-up worker activity at [src/gza/cli/watch.py](/workspace/src/gza/cli/watch.py:1981) and [src/gza/cli/watch.py](/workspace/src/gza/cli/watch.py:2054). |
| `WS-§1-MERGE-NO-DUPLICATES-PER-UNIT` | `_WatchLog` now tracks merge dedupe by unit/owner key via `note_merge_logged()` / `was_merge_logged()` at [src/gza/cli/watch.py](/workspace/src/gza/cli/watch.py:985), and snapshot-based transition logging suppresses repeats for the same merge unit before emitting at [src/gza/cli/watch.py](/workspace/src/gza/cli/watch.py:1060). |

## Findings (divergences)

None.

## Recommendations
1. No follow-up behavior fix is indicated for the scoped merge-logging assertions.
