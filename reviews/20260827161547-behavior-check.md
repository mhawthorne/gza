# Behavior conformance check

**Scope:** lifecycle-engine.md §landing source verify exact tree evidence
**Behavior spec commit:** not inspected; repository instructions for this workspace prohibit git commands in agent context

## Scorecard
Assertions checked: 1 — HOLDS: 1 · DIVERGES: 0 · UNDETERMINED: 0

## Findings (divergences)

None.

## Undetermined (needs a closer look or a human)

| Assertion ID | Why undetermined |
|--------------|------------------|

## Holds (verified conformant)

| Assertion ID | Evidence |
|--------------|----------|
| LE-§LANDING-SOURCE-VERIFY-EXACT-TREE | `specs/behavior/lifecycle-engine.md:921` requires current lifecycle verify evidence for the exact final live source head/tree and current gate identity. `src/gza/landing.py:774` resolves the live source-tree fingerprint before the initial acquisition inspection, and `src/gza/landing.py:826` resolves it again after verify/reconcile execution before reinspection. `src/gza/landing.py:1241` normalizes the expected live tree, and `src/gza/landing.py:1248` marks identity matched only when that nonblank expected tree exactly equals the canonical artifact tree. `src/gza/landing.py:1264` then allows current-green only when identity matched and the artifact tree is present. Regression coverage in `tests/test_landing.py` exercises omitted, blank, static mismatch, stable exact acquisition, and post-execution changed-tree cases. |

## Recommendations

1. No follow-up required for this scoped assertion.

## Machine-readable findings

```json
[
  {
    "assertion_id": "LE-§LANDING-SOURCE-VERIFY-EXACT-TREE",
    "verdict": "HOLDS",
    "recommendation": null,
    "spec_file": "specs/behavior/lifecycle-engine.md",
    "spec_section": "§landing",
    "summary": "Landing source verify evidence requires nonblank live tree identity and exact equality with the canonical artifact tree before it can count as current green.",
    "evidence": [
      {
        "path": "specs/behavior/lifecycle-engine.md",
        "line": 921,
        "note": "Requires current lifecycle verify evidence for exact final live source head/tree and current verify-gate identity."
      },
      {
        "path": "src/gza/landing.py",
        "line": 774,
        "note": "Initial acquisition resolves the live source-tree fingerprint before comparing canonical verify evidence."
      },
      {
        "path": "src/gza/landing.py",
        "line": 826,
        "note": "Post-execution acquisition resolves the live source-tree fingerprint again before reinspection."
      },
      {
        "path": "src/gza/landing.py",
        "line": 1241,
        "note": "Expected live tree fingerprints are normalized before comparison."
      },
      {
        "path": "src/gza/landing.py",
        "line": 1248,
        "note": "Identity only matches when the normalized expected live tree is nonblank and equals the canonical artifact fingerprint."
      },
      {
        "path": "src/gza/landing.py",
        "line": 1264,
        "note": "Current-green evidence requires identity_matched plus present epoch, gate identity, and artifact tree fingerprint."
      }
    ],
    "report_path": "reviews/20260827161547-behavior-check.md"
  }
]
```
