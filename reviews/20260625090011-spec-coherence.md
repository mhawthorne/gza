# Behavior spec coherence check

**Scope:** `specs/behavior/isolated-merge-checkout.md`, `specs/behavior/README.md`
**Owner map reviewed:** `specs/behavior/README.md`, `specs/behavior/00-overview.md`, `specs/behavior/watch-supervisor.md`, `specs/behavior/isolated-merge-checkout.md`

## Summary
Files reviewed: 4
Findings: 0 overlap · 0 restatement · 0 cross-reference · 0 RFC-2119 misuse · 0 plain-language rewrites

## Findings

None.

## Resolved / clean boundaries

| File or boundary | Evidence |
|------------------|----------|
| `README.md` ↔ `isolated-merge-checkout.md` | The new file has a single explicit owner statement, and the index assigns it only the isolated merge staging/promotion contract. |
| `isolated-merge-checkout.md` ↔ `watch-supervisor.md` | The new file defers loop ordering and merge eligibility to the existing owners instead of restating supervisor rules. |
| `isolated-merge-checkout.md` ↔ `00-overview.md` | The new file does not redefine lifecycle-wide invariants; it narrows scope to attached-checkout promotion semantics. |

## Recommendations
1. Keep future operator-facing isolated-promotion rules in `isolated-merge-checkout.md` and reference it from broader watch docs instead of duplicating the contract.
