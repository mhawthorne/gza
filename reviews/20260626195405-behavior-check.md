# Behavior conformance check

**Scope:** `specs/behavior/lineage.md` P1
**Behavior spec commit:** not captured; `AGENTS.md` forbids git commands in this context

## Scorecard
Assertions checked: 1 — HOLDS: 1 · DIVERGES: 0 · UNDETERMINED: 0

## Findings (divergences)

None.

## Undetermined (needs a closer look or a human)

None.

## Holds (verified conformant)

| Assertion ID | Evidence |
|--------------|----------|
| LINEAGE-P1-NEEDS-ATTENTION-OWNER | `specs/behavior/lineage.md:57`-`59` requires operator-facing needs-attention rows to show owners, not lineage leaves. The shared subject-rooting helper in `src/gza/advance_engine.py:2019`-`2024` resolves the owning implement before emitting a subject id, and both previously divergent builders now route through it at `src/gza/advance_engine.py:2265`-`2284` and `2288`-`2300`. Regression coverage verifies both the owner-self case and the rebase-leaf case at `tests/test_advance_engine.py:9732`-`9765` and `9768`-`9806`. |

## Recommendations
1. Keep new needs-attention builders on `_needs_attention_subject_id(ctx)` whenever the current task can be a contributor leaf.
