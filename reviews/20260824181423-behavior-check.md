# Behavior conformance check

**Scope:** lifecycle rebase/landing post-rebase artifact slice
**Behavior spec commit:** unavailable in agent context; AGENTS.md forbids git commands

## Scorecard
Assertions checked: 3 - HOLDS: 3 - DIVERGES: 0 - UNDETERMINED: 0

## Findings (divergences)

None.

## Holds (verified conformant)

| Assertion ID | Evidence |
|--------------|----------|
| LE-§8A-ONE-TASK-BACKED-REBASE | `src/gza/rebase_service.py:133` exposes a synchronous task-backed rebase service that creates/reuses/runs one rebase request; `src/gza/cli/git_ops.py:2528` routes foreground `cmd_rebase` through it while preserving the existing executor. |
| LE-§8A-MECHANICAL-PRESERVES-REVIEW | `src/gza/cli/git_ops.py:2316` records `completed_mechanical` only on the non-provider success path, and `src/gza/cli/git_ops.py:2338` carries the existing `changed_diff` proof into the typed outcome. |
| LE-§8A-PROVIDER-RESOLUTION-REQUIRES-PROOF | `src/gza/cli/git_ops.py:2313` records provider conflict resolution as `provider_conflict_resolved`, and `src/gza/rebase_service.py:386` persists `provider_conflict_resolved` in the `rebase_execution_outcome` artifact so landing can avoid log parsing. |

## Recommendations

1. Wire the landing coordinator to consume `rebase_execution_outcome` exact-key artifacts when the later landing slice integrates rebase acquisition.

## Machine-readable findings

```json
[]
```
