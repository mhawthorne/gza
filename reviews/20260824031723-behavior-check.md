# Behavior conformance check

**Scope:** landing refusal-class mapping for `CHANGES_REQUESTED` review evidence
**Behavior spec commit:** not inspected; repository instructions forbid git commands in this context

## Scorecard

Assertions checked: 4 - HOLDS: 4 - DIVERGES: 0 - UNDETERMINED: 0

## Findings (divergences)

None.

## Holds (verified conformant)

| Assertion ID | Evidence |
|--------------|----------|
| OV-CMDREF-REQUIRED-REVIEW-UNAVAILABLE | `specs/behavior/00-overview.md:269` keeps missing, stale, failed, malformed, unknown, unavailable, `NEEDS_DISCUSSION`, and identity-mismatched review evidence in `required-review-unavailable`, while excluding current parseable plain-full/resolution `CHANGES_REQUESTED` when guarded judgment prerequisites are eligible. `src/gza/landing_policy.py:71` and `src/gza/landing_policy.py:83` classify unusable review evidence as `required-review-unavailable`; `tests/test_landing_policy.py:113` verifies those cases do not invoke the judge. |
| LE-§8A-REQUIRED-REVIEW-UNAVAILABLE | `specs/behavior/lifecycle-engine.md:1165` defines the same item 5 boundary and explicitly states eligible guarded `CHANGES_REQUESTED` evidence is usable escalation evidence. `src/gza/landing_policy.py:84` keeps spec-coherence `CHANGES_REQUESTED` at item 5, while `src/gza/landing_policy.py:88` through `src/gza/landing_policy.py:91` preserve plain-full/resolution `CHANGES_REQUESTED` for strict/guarded policy handling. |
| LE-§8A-NONDEFERRABLE-BLOCKER | `specs/behavior/lifecycle-engine.md:1171` maps strict current plain-full/resolution `CHANGES_REQUESTED` and guarded nondeferrable blockers to item 6. `src/gza/landing_policy.py:93` and `src/gza/landing_policy.py:95` implement those two stops; `tests/test_landing_policy.py:40` and `tests/test_landing_policy.py:55` verify the judge is not invoked. |
| LE-§8A-POLICY-OR-JUDGE-REFUSED | `specs/behavior/lifecycle-engine.md:1175` maps guarded `BLOCK` and `NEEDS_HUMAN` to item 7 while allowing `LAND` to continue. `src/gza/landing_policy.py:97` and `src/gza/landing_policy.py:102` implement guarded disabled/missing/refused judgment handling; `tests/test_landing_policy.py:73` verifies `LAND`, `BLOCK`, and `NEEDS_HUMAN` outcomes. |

## Recommendations

1. Wire `classify_landing_review_policy` into the full landing coordinator when the placeholder `gza land` implementation is completed, so this pure contract becomes the live command path.

## Machine-readable findings

```json
[]
```
