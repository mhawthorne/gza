# Behavior conformance check

**Scope:** shared-schema verify isolation slice
**Behavior spec commit:** not collected; repository instructions prohibit git commands in this agent context

## Scorecard
Assertions checked: 3 — HOLDS: 3 · DIVERGES: 0 · UNDETERMINED: 0

## Findings (divergences)

None.

## Holds (verified conformant)

| Assertion ID | Evidence |
|--------------|----------|
| SSI-SSI5-VERIFY-USES-ISOLATED-DB | `specs/behavior/shared-schema-isolation.md:108` requires verify subprocesses to receive isolated private DB state. `src/gza/runner.py:6776` extends the central verify wrapper with `runtime_context`, and `src/gza/runner.py:6794` routes that context through `disposable_verify_db_snapshot_env` before calling the subprocess runner with `snapshot.env`. |
| SSI-SSI5-FRESH-PER-ATTEMPT | `specs/behavior/shared-schema-isolation.md:116` requires writable verify snapshots to be discarded after each attempt and fresh for reruns. `src/gza/runner.py:3000` creates a new temp directory for each context entry, `src/gza/runner.py:3003` clones with SQLite backup, and `src/gza/runner.py:6794` wraps each central verify call in that context. |
| MV-MV1-SCHEMA-SKEW-INELIGIBLE-FOR-REMEDIATION | `specs/behavior/main-verify-self-heal.md:79` and `specs/behavior/main-verify-self-heal.md:86` classify schema-runtime skew as unavailable and ineligible for both lifecycle and red-main remediation. `src/gza/main_integration_verify.py:1456`/`src/gza/main_integration_verify.py:2037` only launch configured verify when the gate is enabled, while `src/gza/main_integration_verify.py:1465` and `src/gza/main_integration_verify.py:2044` route those launches through the central snapshot-aware verify wrapper; the existing schema-skew normalization remains downstream of the same result path. |

## Recommendations

No behavior divergences found in the scoped check.

## Machine-readable findings

```json
[]
```
