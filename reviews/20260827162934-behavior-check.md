# Behavior conformance check

**Scope:** lifecycle-engine.md post-rebase review bindability assertion
**Behavior spec commit:** not captured; AGENTS.md prohibits git commands in this context

## Scorecard
Assertions checked: 1 — HOLDS: 1 · DIVERGES: 0 · UNDETERMINED: 0

## Findings (divergences)

None.

## Undetermined (needs a closer look or a human)

| Assertion ID | Why undetermined |
|--------------|------------------|

## Holds (verified conformant)

| Assertion ID | Evidence |
|--------------|----------|
| LE-§8A-POST-REBASE-REVIEW-BINDABILITY | `land` falls back from resolution review to full current-head review before review lookup and budget handling when durable rebase provenance cannot be bound; resolution review reuse also requires the review scope to bind to the same persisted rebase provenance. Evidence: `src/gza/landing.py:885`, `src/gza/landing.py:1398`, `src/gza/landing.py:1524`, `src/gza/landing.py:1555`, `src/gza/landing.py:1603`. Regression coverage exercises nonblank unbindable rows, full fallback creation/reuse/blocking, and unbindable resolution-review non-reuse at `tests/test_landing.py:3120`, `tests/test_landing.py:3204`, `tests/test_landing.py:3280`, `tests/test_landing.py:3332`. |

## Recommendations

1. None.

## Machine-readable findings

```json
[
  {
    "assertion_id": "LE-\u00a78A-POST-REBASE-REVIEW-BINDABILITY",
    "verdict": "HOLDS",
    "recommendation": null,
    "spec_file": "specs/behavior/lifecycle-engine.md",
    "spec_section": "\u00a78a",
    "summary": "Post-rebase review selection now uses narrow resolution review only when exact persisted rebase provenance binds to the implementation, resolved head, and target; otherwise it falls back to one full current-head review.",
    "evidence": [
      {
        "path": "specs/behavior/lifecycle-engine.md",
        "line": 1011,
        "note": "Spec requires resolution review when exact provenance can be bound, otherwise full current-head fallback."
      },
      {
        "path": "src/gza/landing.py",
        "line": 885,
        "note": "Review need is downgraded from resolution to full before review lookup when bindability fails."
      },
      {
        "path": "src/gza/landing.py",
        "line": 1398,
        "note": "Bindable identity requires a persisted rebase row."
      },
      {
        "path": "src/gza/landing.py",
        "line": 1401,
        "note": "Persisted row must be a rebase for the implementation."
      },
      {
        "path": "src/gza/landing.py",
        "line": 1403,
        "note": "Persisted rebase provenance must be parseable and complete."
      },
      {
        "path": "src/gza/landing.py",
        "line": 1407,
        "note": "Persisted resolved head and target must match the landing request."
      },
      {
        "path": "src/gza/landing.py",
        "line": 1603,
        "note": "Resolution-review reuse parses durable review metadata before accepting a match."
      },
      {
        "path": "src/gza/landing.py",
        "line": 1616,
        "note": "Resolution-review reuse also revalidates the referenced persisted rebase provenance."
      },
      {
        "path": "tests/test_landing.py",
        "line": 3120,
        "note": "Regression matrix covers nonblank unbindable rebase identities falling back to full review creation."
      },
      {
        "path": "tests/test_landing.py",
        "line": 3204,
        "note": "Regression matrix covers spent-budget reuse of exact completed full fallback reviews."
      },
      {
        "path": "tests/test_landing.py",
        "line": 3280,
        "note": "Regression matrix covers spent-budget blocking without exact full fallback evidence."
      },
      {
        "path": "tests/test_landing.py",
        "line": 3332,
        "note": "Regression test proves unbindable resolution review scopes are not reused."
      }
    ],
    "report_path": "reviews/20260827162934-behavior-check.md"
  }
]
```
