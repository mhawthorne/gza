# Behavior conformance check

**Scope:** `WS` phase 5 / watch recovery start confirmation assertions touched by this task
**Behavior spec commit:** not recorded in agent context (`git` commands are disabled by repo rules)

## Scorecard
Assertions checked: 3 — HOLDS: 3 · DIVERGES: 0 · UNDETERMINED: 0

## Findings (divergences)

None.

## Undetermined (needs a closer look or a human)

None.

## Holds (verified conformant)

| Assertion ID | Evidence |
|--------------|----------|
| `WS-S6-START-CONFIRMED-ONLY` | Detector-owned start confirmation is derived from snapshot diffs in [src/gza/cli/watch.py](/workspace/src/gza/cli/watch.py:1880) and boundary confirmation/no-show handling in [src/gza/cli/watch.py](/workspace/src/gza/cli/watch.py:2369). Recovery launches no longer emit a live success line on spawn success alone; they are queued in `expected_starts` at [src/gza/cli/watch.py](/workspace/src/gza/cli/watch.py:4203) and confirmed only at cycle boundaries in [src/gza/cli/watch.py](/workspace/src/gza/cli/watch.py:4714). |
| `WS-S6-NO-SHOW-WARNING` | A launch that never reaches `in_progress` and has no live worker emits `START_FAILED ... spawned worker never reached in_progress` in [src/gza/cli/watch.py](/workspace/src/gza/cli/watch.py:2407), rather than a clean `START`. |
| `WS-S6-OUTCOME-VISIBLE-EACH-CYCLE` | The cycle summary still surfaces stable human-required attention every pass, but unchanged content is de-emphasized to a one-line `INFO` in [src/gza/cli/watch.py](/workspace/src/gza/cli/watch.py:2355), preserving visibility without reprinting the full block each cycle. |

## Recommendations
1. Keep migrating remaining inline START paths toward the same detector-owned confirmation seam so queued starts and recovery starts eventually share one confirmation model.
