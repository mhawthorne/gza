# Behavior conformance check

**Scope:** `specs/behavior/lifecycle-engine.md` manual merge red verify-gate assertions
**Behavior spec commit:** not inspected; repository instructions for this task prohibit git commands

## Scorecard
Assertions checked: 5 - HOLDS: 5 - DIVERGES: 0 - UNDETERMINED: 0

## Findings (divergences)
None.

## Undetermined (needs a closer look or a human)
None.

## Holds (verified conformant)

| Assertion ID | Evidence |
|--------------|----------|
| LE-MERGE-RED-VERIFY-TWO-KEY | `src/gza/cli/git_ops.py:1645` checks `is_current_red_verify_gate_action` before generic attention force; `src/gza/cli/git_ops.py:1646-1652` refuses unless both `--force` and `--ignore-verify-gate` are present. |
| LE-MERGE-RED-VERIFY-MATRIX | `src/gza/advance_engine.py:3393-3416` centralizes the allowed current-red action predicate for `create_verify_fix`, `run_verify_fix`, `wait_verify_fix`, `rerun_completed_verify_fix`, and same-epoch `needs_discussion` with reason `verify-fix-failed`. |
| LE-MERGE-RED-VERIFY-WARNING | `src/gza/cli/git_ops.py:1659-1665` prints the red-gate warning with failing epoch head and verify command. |
| LE-MERGE-RED-VERIFY-PROVENANCE | `src/gza/cli/git_ops.py:1653-1657` switches ordinary manual provenance to `manual_force` for successful red-gate bypasses. |
| LE-MERGE-ABSOLUTE-REFUSALS | `src/gza/cli/git_ops.py:1715-1723` refuses real git conflicts after red-gate acknowledgement; `src/gza/cli/git_ops.py:1725-1732` still calls deferred-blocker materialization/refusal before merge execution. |

## Recommendations
None.

## Machine-readable findings

```json
[]
```
