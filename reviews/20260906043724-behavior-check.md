# Behavior conformance check

**Scope:** OV-INV3/LE-§8a deferred-blocker materialization identity
**Behavior spec commit:** not collected; repository policy forbids git commands in this agent context

## Scorecard

Assertions checked: 3 - HOLDS: 3 - DIVERGES: 0 - UNDETERMINED: 0

## Findings (divergences)

None.

## Holds (verified conformant)

| Assertion ID | Evidence |
|--------------|----------|
| OV-INV3-DEFERRED-BLOCKERS-BEFORE-MERGE | `specs/behavior/00-overview.md:190` requires every deferred-blocker task before merge success. `src/gza/merge_services.py:901` validates materialized deferred blocker rows before merge state mutation and returns `deferred_blocker_materialization_failed` on mismatch. |
| LE-§8A-EXACT-DEFERRED-BLOCKER-IDENTITY | `specs/behavior/lifecycle-engine.md:1271` requires urgent PR-required deferred-blocker tasks for every current authorized blocker. `src/gza/landing.py:2028` builds `MergeLandingAuthorization` with per-blocker deferred task prompt and review-scope content hashes. |
| LE-§8A-REUSE-EXACT-CURRENT-ARTIFACTS | `specs/behavior/lifecycle-engine.md:1343` requires exact-key/deferred artifacts to be reused only when current. `src/gza/review_tasks.py:2409` filters deferred blocker reuse by exact prompt and review scope when supplied, and `src/gza/merge_services.py:935` independently rejects stale prompt or review-scope hashes at the merge boundary. |

## Recommendations

No follow-up from this scoped check.

## Machine-readable findings

```json
[]
```
