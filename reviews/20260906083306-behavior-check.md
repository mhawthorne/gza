# Behavior conformance check

**Scope:** `shared-schema-isolation.md` SSI5 Docker verify traversal leases
**Behavior spec commit:** not collected; repository instructions prohibit git commands in this context

## Assertion Inventory

| Assertion ID | Assertion |
|--------------|-----------|
| SSI-SSI5-DOCKER-TRAVERSAL-LEASES | Docker verify snapshot traversal permissions on pre-existing checkout path components MUST be coordinated across overlapping snapshot attempts, including separate gza processes, and exact original modes MUST be restored only after the last active dependent lease exits or dead holders are pruned. |

## Scorecard

Assertions checked: 1 - HOLDS: 1 - DIVERGES: 0 - UNDETERMINED: 0

## Findings (divergences)

None.

## Undetermined (needs a closer look or a human)

None.

## Holds (verified conformant)

| Assertion ID | Evidence |
|--------------|----------|
| SSI-SSI5-DOCKER-TRAVERSAL-LEASES | The spec requires cross-process coordination and exact restoration at `specs/behavior/shared-schema-isolation.md:121`. The implementation stores checkout-scoped lease state under `.gza/tmp`, serializes access with `fcntl.flock`, records holder PID/token and original mode, and prunes dead holders at `src/gza/runner.py:2967`, `src/gza/runner.py:3020`, `src/gza/runner.py:3043`, and `src/gza/runner.py:3066`. Process regression coverage verifies first-process exit while a second process remains active, abrupt holder death, and partial setup cleanup at `tests/test_runner.py:7041`, `tests/test_runner.py:7095`, and `tests/test_runner.py:7162`. |

## Recommendations

No follow-up required for the scoped assertion.

## Machine-readable findings

```json
[]
```
