# Behavior conformance check

**Scope:** landing review follow-up evidence contract in `00-overview.md` and `lifecycle-engine.md`
**Behavior spec commit:** unavailable in agent context; repository guidance forbids git commands here

## Scorecard
Assertions checked: 4 - HOLDS: 4 - DIVERGES: 0 - UNDETERMINED: 0

## Findings (divergences)

None.

## Holds (verified conformant)

| Assertion ID | Evidence |
|--------------|----------|
| OV-INV3-FOLLOWUPS-DURABLE-BEFORE-MERGE | `specs/behavior/00-overview.md:172` requires `APPROVED_WITH_FOLLOWUPS` to preserve follow-up work before merge. Landing follow-up evidence is normalized at `src/gza/landing.py:228`, validated into a durable review/source/finding/content identity at `src/gza/landing.py:242`, and exposed as merge materialization input only on allowed policy decisions at `src/gza/landing.py:1006`; `tests/test_landing.py:283` covers the valid materialization path. |
| LE-S6-AWF-ZERO-FOLLOWUPS-NEEDS-DISCUSSION | `specs/behavior/lifecycle-engine.md:510` requires `APPROVED_WITH_FOLLOWUPS` with zero parsed follow-ups to fail closed. Landing now refuses zero, missing, blank, or malformed follow-up identity evidence through `_followup_materialization_identities` at `src/gza/landing.py:719` and `_review_availability_block` at `src/gza/landing.py:1123`; `tests/test_landing.py:274` and `tests/test_landing.py:358` cover zero and malformed evidence. |
| LE-S8A-VALID-AWF-MERGE-PERMITTING | `specs/behavior/lifecycle-engine.md:1014` says valid `APPROVED_WITH_FOLLOWUPS` remains merge-permitting. The landing policy accepts `APPROVED_WITH_FOLLOWUPS` after availability and consistency checks at `src/gza/landing.py:1006` and returns exact materialization identities; `tests/test_landing.py:283` and `tests/test_landing.py:401` cover valid and normalization-equivalent inputs. |
| LE-S8A-VISITED-FINGERPRINT-REVIEW-IDENTITY | `specs/behavior/lifecycle-engine.md:1117` requires decision-relevant review identity in the landing fingerprint. `LandingReviewFingerprint` includes canonical follow-up identity fingerprints at `src/gza/landing.py:426`, `LandingStateFingerprint.from_facts` includes them at `src/gza/landing.py:491`, and the canonical encoding at `src/gza/landing.py:274` prevents delimiter collisions; `tests/test_landing.py:1158`, `tests/test_landing.py:1221`, and `tests/test_landing.py:1263` cover ordering stability, every material identity field, and delimiter-bearing identities. |

## Recommendations

No follow-up from this scoped behavior check.

## Machine-readable findings

```json
[]
```
