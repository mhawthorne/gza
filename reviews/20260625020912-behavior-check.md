# Behavior conformance check

**Scope:** `specs/behavior/off-topic-verify-failures.md` and the matching `A2` assertion in `specs/behavior/lifecycle-engine.md`
**Behavior spec commit:** not recorded in-agent (`git` commands are disabled by `AGENTS.md`)
**Invocation:** manual fallback for `uv run gza-behavior-check`

## Scorecard
Assertions checked: 6 — HOLDS: 6 · DIVERGES: 0 · UNDETERMINED: 0

## Findings (divergences)

None.

## Holds (verified conformant)

| Assertion ID | Evidence |
|--------------|----------|
| `OTV-REPRODUCE-OR-RECORD-CONTRACT` | Investigation prompts now require red-under-stress evidence before fixing, green on the same harness after fixing, and a structured inconclusive record when budget expires at [src/gza/flaky_investigations.py](/workspace/src/gza/flaky_investigations.py:111). |
| `OTV-NO-SPECULATIVE-FLAKE-SUPPRESSIONS` | The prompt explicitly forbids blanket sleeps, blanket retries, `@flaky`, and broad timeout increases at [src/gza/flaky_investigations.py](/workspace/src/gza/flaky_investigations.py:125). |
| `OTV-BOUNDED-TARGETED-HARNESS` | The reproduce helper builds one bounded targeted plan from persisted investigation metadata, caps runs to 20, preserves the recorded project cwd, and reuses the targeted failing-node command at [src/gza/flaky_investigations.py](/workspace/src/gza/flaky_investigations.py:173) and [src/gza/flaky_investigations.py](/workspace/src/gza/flaky_investigations.py:694). |
| `OTV-FAULTHANDLER-AND-OPTIONAL-STRESS-FLAGS` | The harness prefixes `PYTHONFAULTHANDLER=1` and only adds xdist/randomization flags when those plugins are available at [src/gza/flaky_investigations.py](/workspace/src/gza/flaky_investigations.py:220), [src/gza/flaky_investigations.py](/workspace/src/gza/flaky_investigations.py:226), and [src/gza/flaky_investigations.py](/workspace/src/gza/flaky_investigations.py:689). |
| `OTV-ATTEMPT-AND-INCONCLUSIVE-EVIDENCE` | Each reproduce run persists `flaky_verify_attempt` artifacts and a budget-exhausted no-repro path persists one `flaky_verify_inconclusive` artifact with attempt IDs and hypotheses at [src/gza/flaky_investigations.py](/workspace/src/gza/flaky_investigations.py:253), [src/gza/flaky_investigations.py](/workspace/src/gza/flaky_investigations.py:541), and [src/gza/flaky_investigations.py](/workspace/src/gza/flaky_investigations.py:631). |
| `LE-A2-INVESTIGATION-ENTRYPOINT-AUDITABILITY` | Lifecycle still clears the review only after durable investigation creation/reuse in [src/gza/cli/advance_executor.py](/workspace/src/gza/cli/advance_executor.py:976), and the first-class operator entrypoint for the downstream reproduce lane is registered at [src/gza/cli/main.py](/workspace/src/gza/cli/main.py:944) and implemented at [src/gza/cli/flaky.py](/workspace/src/gza/cli/flaky.py:18). |

## Recommendations
1. Restore a packaged `gza-behavior-check` executable or wrapper so this check no longer needs the manual fallback path.
