# Behavior conformance check

**Scope:** `specs/behavior/watch-supervisor.md` fleet dispatch and recovery-slot assertions touched by this change
**Behavior spec commit:** not inspected; repository AGENTS forbids git commands in this context

## Scorecard

Assertions checked: 7 - HOLDS: 7 - DIVERGES: 0 - UNDETERMINED: 0

## Findings (divergences)

None.

## Holds (verified conformant)

| Assertion ID | Evidence |
|--------------|----------|
| WS-§GLOBAL-BARRIER-DISPATCH-SETTLE-REFRESH | Spec requires strategy-selected per-project heads to dispatch through the selected runtime, settle, refresh occupancy, and continue (`specs/behavior/watch-supervisor.md:140`). `dispatch_watch_supervisor_lane_incrementally` chooses heads via strategy and dispatches through the selected runtime (`src/gza/cli/watch.py:9750`, `src/gza/cli/watch.py:9808`); `SupervisorLaunchBudget.dispatch_recovery_candidate` reserves, dispatches, refreshes, and releases non-live reservations (`src/gza/cli/watch.py:9544`). |
| WS-§AGG-RESERVATION-COVERS-SETTLE | Spec requires provisional reservations to cover the plan-to-live window (`specs/behavior/watch-supervisor.md:174`). Recovery dispatch reserves before runtime-local launch (`src/gza/cli/watch.py:9556`) and holds through settlement/refresh (`src/gza/cli/watch.py:9568`, `src/gza/cli/watch.py:9585`). |
| WS-§AGG-RELEASE-NONLIVE | Spec requires terminal/no-live-proof outcomes to release the provisional reservation and allow another candidate (`specs/behavior/watch-supervisor.md:184`). The budget refreshes after settlement and releases when `slot_consuming` is false (`src/gza/cli/watch.py:9585`); runtime maps terminal and no-live-proof to non-slot-consuming results (`src/gza/cli/watch.py:11924`, `src/gza/cli/watch.py:11934`). |
| WS-§LOCAL-PERMIT-REQUIRED | Spec keeps project-local launch permits required (`specs/behavior/watch-supervisor.md:191`). Runtime recovery dispatch acquires the owning project's `launch_permit` before preparing/spawning recovery work (`src/gza/cli/watch.py:11654`). |
| WS-§RECOVERY-SAME-FLEET-BUDGET | Spec requires worker-consuming recovery to use the same fleet budget and not multiply per project (`specs/behavior/watch-supervisor.md:195`). Lane allocation resolves a single recovery limit and the fleet cycle calls one incremental lanes dispatcher with one `SupervisorLaunchBudget` (`src/gza/cli/watch.py:9835`, `src/gza/cli/watch.py:10425`). |
| WS-§LANES-HEAD-STRATEGY | Spec requires recovery and pending lanes to arbitrate only current per-project heads (`specs/behavior/watch-supervisor.md:216`). `_watch_supervisor_lane_head` asks each runtime for one lane head, and the strategy selects from the `heads` mapping (`src/gza/cli/watch.py:9648`, `src/gza/cli/watch.py:9789`). |
| WS-§NO-GLOBAL-QUEUE-POSITION | Spec forbids global `queue_position` arbitration (`specs/behavior/watch-supervisor.md:245`). The changed dispatch path does not introduce queue-position writes/comparisons; cross-project choice remains `strategy.select_next(heads)` (`src/gza/cli/watch.py:9808`). |

## Recommendations

None for this slice.

## Machine-readable findings

```json
[]
```
