# Behavior conformance check

**Scope:** `shared-schema-isolation.md` verify-snapshot assertions (`SSI5`)
**Behavior spec commit:** unavailable in agent context (git commands disabled by repository instructions)

## Scorecard

Assertions checked: 4 - HOLDS: 4 - DIVERGES: 0 - UNDETERMINED: 0

## Findings (divergences)

None.

## Holds (verified conformant)

| Assertion ID | Evidence |
|--------------|----------|
| SSI5-VERIFY-ISOLATED-COPY | `_run_review_verify_command` refuses missing `runtime_context` and always enters `disposable_verify_db_snapshot_env` before launching verify (`src/gza/runner.py:6792`). |
| SSI5-FRESH-PER-ATTEMPT | Direct verify recovery, local-target baselines, and flaky reproduction now invoke `_run_review_verify_command` with the owning runtime context for each attempt (`src/gza/cli/advance_executor.py:2216`, `src/gza/off_topic_verify.py:685`, `src/gza/flaky_investigations.py:306`). |
| SSI5-WRITES-DISCARDED | `disposable_verify_db_snapshot_env` clones with SQLite backup, routes `GZA_DB_PATH` to the clone, and removes the DB plus `-wal`, `-shm`, and `-journal` companions in `finally` (`src/gza/runner.py:2997`, `src/gza/runner.py:3034`). |
| SSI5-HOST-PERSISTENCE-SEPARATE | Verify result capture remains outside the snapshot context and persists only explicit host-side result/artifact evidence after the child returns (`src/gza/cli/advance_executor.py:2243`, `src/gza/flaky_investigations.py:326`). |

## Recommendations

None for this scope.

## Machine-readable findings

```json
[]
```
