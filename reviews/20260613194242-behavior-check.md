# Behavior conformance check

**Scope:** edited no-work merge-state assertions in `lineage.md`, `lifecycle-engine.md`, and `recovery.md`
**Behavior spec commit:** not inspected; git commands are disabled by repository instructions

## Scorecard
Assertions checked: 8 — HOLDS: 8 · DIVERGES: 0 · UNDETERMINED: 0

## Findings (divergences)

None.

## Holds (verified conformant)

| Assertion ID | Evidence |
|--------------|----------|
| LIN-MERGE-STATES-FOUR-STATES | `MergeBranchState` includes `redundant` at `src/gza/merge_state.py:14`; persisted legacy mapping keeps `redundant` out of legacy `merge_status` at `src/gza/db.py:224`. |
| LIN-MERGE-STATES-PROVENANCE-SPLIT | Zero unique commits split by `source_has_commits` at `src/gza/merge_state.py:287`; task provenance is passed from task resolution at `src/gza/merge_state.py:394` and sync reconciliation at `src/gza/sync_ops.py:365`. |
| LIN-DEPENDENCY-REDUNDANT-TERMINAL | Dependency readiness accepts `redundant` through the same release policy as `empty` at `src/gza/dependency_preconditions.py:113` and `src/gza/dependency_preconditions.py:294`. |
| LE-TERMINAL-NO-WORK | `TERMINAL_MERGE_STATES` includes `redundant` at `src/gza/lifecycle_completion.py:7`; incomplete/unmerged live filtering uses that set at `src/gza/cli/query.py:2495`. |
| LE-NO-REVIEW-FOR-REDUNDANT | Auto-review suppression covers `redundant` at `src/gza/lifecycle_completion.py:47` and `src/gza/lifecycle_completion.py:65`. |
| REC-NO-WORK-NOT-LANDED | Recovery branch reachability classification receives `source_has_commits` at `src/gza/recovery_engine.py:820`; terminal `redundant` alone is handled by recovery evidence checks rather than landed suppression at `src/gza/recovery_engine.py:1173` and `src/gza/recovery_engine.py:1182`. |
| REC-MOOT-REDUNDANT-OMITTED | All-zero/no-execution redundant rows skip with distinct `merge_unit_redundant` text at `src/gza/recovery_engine.py:1341`; failed-task listing omits only moot redundant rows at `src/gza/recovery_engine.py:1182`. |
| REC-RECOVERABLE-REDUNDANT-RETAINED | The recovery list predicate keeps redundant rows with executed/maybe-executed sessions recoverable at `src/gza/recovery_engine.py:1173` and `src/gza/recovery_engine.py:1182`, matching the fail-closed provider-evidence rule. |

## Recommendations

1. No follow-up required from this scoped conformance pass.
