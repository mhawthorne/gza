# Behavior conformance check

**Scope:** `specs/behavior/shared-schema-isolation.md` assertions relevant to explicit `gza migrate`
**Behavior spec commit:** unavailable; repository instructions forbid git commands in this agent context.

## Scorecard
Assertions checked: 4 - HOLDS: 4 - DIVERGES: 0 - UNDETERMINED: 0

## Findings (divergences)

None.

## Holds (verified conformant)

| Assertion ID | Evidence |
|--------------|----------|
| SSI-SSI2-EXPLICIT-MIGRATE-AUTHORITY | `specs/behavior/shared-schema-isolation.md:82` says explicit operator migrations use the same shared-DB authority rule. `_cmd_migrate` in `src/gza/cli/main.py` classifies shared DBs through `configured_db_is_shared`, resolves canonical authority for write-capable shared migrations before `check_migration_status`, and refuses absent proof before DB openers or manual helpers run. |
| SSI-SSI2-PROOF-REVALIDATED-BEFORE-MUTATION | `specs/behavior/shared-schema-isolation.md:78` requires revalidation before shared migration writes. `_cmd_migrate` performs initial CLI preflight, and shared manual migrations route through `run_manual_migration_with_authority` in `src/gza/db.py`, which acquires `_schema_bootstrap_lock`, revalidates the bound proof while holding it, and only then invokes the existing v25/v26/v27 writable helpers. The store bootstrap path in `src/gza/db.py` continues to revalidate inside `_ensure_db` before automatic migration/bootstrap writes. |
| SSI-SSI3-MISSING-AUTHORITY-NO-MUTATION | `specs/behavior/shared-schema-isolation.md:88` forbids shared schema/current-version mutation without authority. `_cmd_migrate` returns on missing shared authority before status inspection, store construction, backups, or manual migration helpers; status and dry-run inspection use read-only SQLite opens, and the deferred shared store path in `src/gza/db.py` still validates through a read-only URI without applying migrations. |
| SSI-SSI2-MANUAL-CONFIRMATION-NOT-AUTHORITY | `specs/behavior/shared-schema-isolation.md:82` says manual confirmation is separate from authority. `_cmd_migrate` checks shared authority independently of `--yes`, while the existing confirmation prompt still runs before manual migrations when `--yes` is absent. |

## Recommendations

No behavior divergences found for this slice.

## Machine-readable findings

```json
[]
```
