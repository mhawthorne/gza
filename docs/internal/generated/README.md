# Generated docs

Files in this directory are derived from source (`src/gza/`), not hand-authored.
Do not edit them directly — a hand edit will be silently overwritten or will
drift the moment the described code changes again. If the content is wrong,
fix it by regenerating from source (or updating the generation process if the
process itself is wrong), not by editing the file in place.

- `watch-cycle-phases.md` — describes `gza watch`'s cycle phases, derived from
  `src/gza/cli/watch.py`.
