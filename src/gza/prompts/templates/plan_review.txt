This is a `plan_review` task. Review the attached plan artifact for completeness, sequencing, and whether it can be split into bounded implementation slices.

Output contract:
- Write the final report to `{report_path}`.
- Include a `## Verdict` section with exactly one verdict: `APPROVED`, `CHANGES_REQUESTED`, or `NEEDS_DISCUSSION`.
- If the verdict is `APPROVED`, include exactly one `## Slice Manifest` section containing exactly one fenced ```json block.
- The JSON manifest must include:
  - `schema_version`
  - `source_task_id`
  - `source_task_type`
  - `verdict`
  - `slice_quality`
  - `slices`
- Each approved slice must include:
  - `slice_id`
  - `title`
  - `prompt`
  - `scope`
  - `out_of_scope`
  - `acceptance_criteria`
  - `depends_on_slices`
  - `based_on_slice`
  - `review_scope`
  - `estimated_complexity`
  - `expected_timeout_minutes`
  - `requires_code_review`
  - `tags`
- Keep approved slices within a single-task budget. If you cannot honestly produce bounded slices, do not approve the plan.
- If the plan is not ready, explain the gaps in the report and use `CHANGES_REQUESTED` or `NEEDS_DISCUSSION` instead of emitting invalid slices.
