# Gza Configuration

project_name: gza

verify_command: 'uv run ruff check src/gza/ && uv run ty check src/gza/ && uv run mypy src/ && uv run python -m checks && uv run pytest tests/ -x -q'
docker_setup_command: 'uv sync'

branch_strategy: date_slug

max_steps: 100

learnings_max_items: 50

merge_squash_threshold: 1

max_review_cycles: 10

#provider: claude
#model: claude-sonnet-4-6
provider: codex
model: gpt-5.3-codex

#task_types:
#  plan:
#    model: claude-opus-4-6
#  review:
#    model: claude-haiku-4-5-20251001
project_id: gzarepo01
